extends layout

block content
  h1= title
  p hello,i invite you to my page.
  p Welcome to the city of dreams. myself #{title}.
  button(type="button" class="btn btn-secondary" onclick="doReduce()") Press Me
  br
  a(href='http://localhost:3000/Recipes') Click Here to Visit Recipes Website
  div#reduce Reduce the value
  script.
    let count = 20;
    let change = -1;
    function doReduce() {
      count += change;
      change -= 1;
      document.getElementById('reduce').textContent = `Count ${count} with change ${change}`;
    }